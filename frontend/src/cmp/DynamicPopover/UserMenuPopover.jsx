import Avatar from '@mui/material/Avatar';

export function UserMenuPopover() {
    return (
        <div>hello user</div>
        // <Avatar alt={loggedInUser.initials} src={loggedInUser.imgUrl} style={{ height: '2.86rem', width: '2.86rem' }} />
    )
}